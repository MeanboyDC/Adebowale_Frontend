
// Arithmetric operators + * - /
// Comparison operators == != > <
// Logical operators 
    And-&&  both of them
    or- ||  one of them
    Not-! either one or none
// Assignment operators 
        assign = 
        add and assign =+
        subtract and assign -=
// Unary operators  
        decrement --
        increment ++ 

// Tinary operators
    condition ? value1 : value2

CONTROL FLOW
        1. conditional statements if .. else statement
        2. Loops - it Loops through until a condition is met e.g for, while, do..while 

String method
        .slice
        .replace
        .tolowercase
        .touppercase
        .repeat
        .indexof
        .substring
        .lastindexof

Number method
        .tofixed(decimals)
        .isInteger(to confirm is a number data is and integal number)
        .floor(rounds number data up to the nearest integal)


Loops
        for

ARRAYS AND FUNCTIONS
ARRAYS [] 
        Arrays of Numbers (const class_score)
        similar items
        items have index number
        add items
        remove items
        change items
        arrys can have the same or different data types
FUCTIONS
        takes input (parameters or auguments)
        give output
        helps to devide your codes to modules
        it is reuseable
ARRAY METHODS
        .push() add elements to Arrays  
        .pop() remove elements from an arrays
        .splice() to replace or remove elements from an array
        .concat() to join two arrays  
        .map() map out elements in an array
        .sort() sorts/arrange elements of an array
        .filter() filter out  elements in an array that meets specific condition

